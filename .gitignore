# 依赖
node_modules/
.pnp/
.pnp.js

# 构建产物
dist/
build/
*.local

# 环境变量
.env
.env.local
.env.*.local

# 数据库
*.db
*.db-journal

# 日志
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# AI 协作（不提交对话记录）
.ai/conversation-logs/
.ai/temp/

# 临时探索（不提交草稿）
explorations/temp/
scratch/
*.draft.md

# 仅本地保留，不入公开仓库（2026-07-16 决定：开发脚手架/未发布内容/竞品调研/旧项目/
# 尚不成熟的架构文档——公开仓库只保留代码 + prompt + skills，这些留本地）
handoff/            # 会话交接脚手架（含真名与内部决策）
docs/               # 架构/调研文档，暂不成熟，本地打磨
drafts/             # 未发布内容草稿（发布前不公开）
archive/            # 上一代废弃项目 + 旧真实数据
iterations/         # 开发迭代日志
reference/video-digests/   # 第三方视频转写精读（版权灰色 + 个人笔记）

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 构建缓存
.cache/
.temp/
.parcel-cache/

# 测试覆盖率
coverage/
.nyc_output/
backend/data/

# Claude Code 本地会话配置（含个人权限白名单，永不入库）
.claude/settings.local.json
