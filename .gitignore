# 依赖
node_modules/
.pnp/
.pnp.js

# 构建产物
dist/
build/
*.local

# 环境变量
.env
.env.local
.env.*.local

# 数据库
*.db
*.db-journal

# 日志
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# AI 协作（不提交对话记录）
.ai/conversation-logs/
.ai/temp/

# 临时探索（不提交草稿）
scratch/
*.draft.md

# 仅本地保留，不入公开仓库（2026-07-16 决定：开发脚手架/未发布内容/竞品调研/旧项目/
# 尚不成熟的架构文档——公开仓库只保留代码 + prompt + skills，这些留本地）
# 注意：gitignore 不支持行内注释，路径必须独占一行（否则规则失效）
# handoff=交接脚手架(含真名/内部决策) docs=架构调研 drafts=未发布 archive=旧项目
# iterations=迭代日志 video-digests=第三方视频转写(版权灰色)
handoff/
docs/
archive/
reference/video-digests/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 构建缓存
.cache/
.temp/
.parcel-cache/
__pycache__/

# 测试覆盖率
coverage/
.nyc_output/
backend/data/

# Claude Code 本地会话配置（含个人权限白名单，永不入库）
.claude/settings.local.json

# 个人认知资产（GitHub 发布原则：只推代码/prompt/skills）
# 创作类文件（drafts/published/templates）已于 2026-07-28 迁往 ~/Documents/项目/writing/
reference/opinions.md
reference/platform-playbooks/

# 系列交接契约/规范（handoff，只留本地——ADR-052 / github-publish-policy）
reference/series-template/INTEGRATION-SPEC.md
reference/series-template/SERIES-SPEC.md
reference/series-template/vendor/VENDOR.md

# 系列样例/封面样张含真实文章内容（创作产物，只留本地——ADR-057 / github-publish-policy）
reference/series-template/covers/
reference/series-template/samples/

# feed-digest skill 的运行时状态（已见文章指纹，非代码）
.claude/skills/feed-digest/data/

# hyperframes 研究整目录：外部仓 clone + 创作视频/配音/品牌页成品（创作产物留本地——ADR-057）
prototype/tutorial-viz/hyperframes-研究/

# .agents 镜像里 feed-digest 的运行时状态（同 .claude 侧规则）
.agents/skills/feed-digest/data/
